# แนะนำ: ตั้ง Python version = 3.12 ในหน้า Advanced settings ตอน deploy
# เพื่อให้ทุกแพ็กเกจใช้ wheel สำเร็จรูป (build เร็ว ไม่ค้าง)
streamlit==1.38.0
Orange3>=3.36            # ใช้โหลดโมเดล .pkcls และทำนาย
orange3-imageanalytics   # ตัวฝังภาพ Inception v3 (image embedding 2048 มิติ)
PyQt5                     # จำเป็นสำหรับ orange3-imageanalytics
numpy                    # ปล่อยให้ pip เลือกให้เข้ากับ Python ที่ใช้
pandas                   # ปล่อยให้ pip เลือกเวอร์ชันที่มี wheel
joblib                   # ใช้โหลดไฟล์โมเดล
Pillow                   # ใช้อ่าน/แสดงไฟล์ภาพ
